function App() {
  return (
    <main className="grid min-h-screen place-items-center px-6 py-10 sm:px-8">
      <section className="w-full max-w-3xl">
        <p className="mb-3 text-sm font-bold uppercase tracking-wide text-transit-teal">
          Public transit routing
        </p>
        <h1 className="text-5xl font-bold leading-none text-transit-blue sm:text-7xl">
          Find the Best Public Transit Route
        </h1>
        <p className="mt-5 max-w-2xl text-readable text-transit-muted">
          The frontend workspace is ready. Route search controls, API data loading, and
          results will be added in upcoming issues.
        </p>
      </section>
    </main>
  );
}

export default App;
