function App() {
  return (
    <main className="app-shell">
      <section className="intro">
        <p className="eyebrow">Public transit routing</p>
        <h1>Find the Best Public Transit Route</h1>
        <p>
          The frontend workspace is ready. Route search controls, API data
          loading, and results will be added in upcoming issues.
        </p>
      </section>
    </main>
  );
}

export default App;
